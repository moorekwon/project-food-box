server {
    listen              8000;

    server_name         localhost;

    charset             utf-8;

    location / {
                        proxy_pass http://unix:/run/foodbox.sock;
    }

    location /static/ {
        alias           /srv/foodbox/.static/;
    }
}